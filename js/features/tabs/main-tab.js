// Main Tab - Wrapper for the emotion wheel (primary feature)
// The wheel is always initialized and running, this module just logs activation

window.MainTab = class MainTab {
    constructor() {
        // No initialization needed - wheel is already set up in main.js
    }

    init() {
        console.log('📍 Main tab active');
        // No other logic needed - the wheel is always running
        // The wheel renderer, state manager, and all wheel functionality
        // are initialized globally in main.js and always available
    }
}
